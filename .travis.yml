language: node_js
node_js:
   - 8

branches:
   only:
      - master

before_script:
   - npm install -g @angular/cli

script:
   - ng build --prod

after_script:
  - cd dist
  - git init
  - git add .
  - git commit -m "New Release"
  - git remote add azure $repo
  - git push azure master
